<div id="message<%= message.id %>">
  <small>
    <strong><%= message.user.first_name %></strong>
    <i><%= message.created_at.strftime("%m-%e %H:%M") %></i>
  </small>
  <p class="d-flex align-items-center"><%= message.content %></p>
</div>
